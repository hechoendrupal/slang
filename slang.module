<?php

/**
 * hook_menu
 */
function slang_menu(){

  $items = array();
  
  $items['/admin/config/slang'] = array(
    'title' => 'Slang Settings',
    'description' => 'Slang settings.',
    'route_name' => 'slang_settings_form',
  );

  return $items;
}

